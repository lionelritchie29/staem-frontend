export interface GameSystemRequirement {
    note: string
    isRecommended: boolean
    operatingSystem: string
    processor: string
    memory: string
    graphics: string
    dirextX: string
    storage: string
}
