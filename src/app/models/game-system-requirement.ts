export interface GameSystemRequirement {
    note: string
    isRecommended: boolean
    operatingSystem: string
    processor: string
    memory: string
    graphics: string
    directX: string
    storage: string
}
